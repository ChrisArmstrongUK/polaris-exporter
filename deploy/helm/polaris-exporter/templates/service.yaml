apiVersion: v1
kind: Service
metadata:
  name: {{ include "polaris-exporter.fullname" . }}
  labels:
    {{- include "polaris-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "polaris-exporter.selectorLabels" . | nindent 4 }}
